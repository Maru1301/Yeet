# Reference only — real values are never stored here.
# The Secret is created imperatively by:
#   - deploy-local.sh       (local dev)
#   - .github/workflows/deploy.yml  (CI/CD)
apiVersion: v1
kind: Secret
metadata:
  name: yeet-secret
type: Opaque
stringData:
  api-key: ""       # set via script/CI from local-env.sh or GitHub Secrets
  mongo-uri: ""     # set via script/CI from local-env.sh or GitHub Secrets
