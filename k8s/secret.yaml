apiVersion: v1
kind: Secret
metadata:
  name: yeet-secret
type: Opaque
stringData:
  api-key: "REPLACE_WITH_YOUR_GOOGLE_AI_STUDIO_KEY"
  mongo-uri: "mongodb://mongo:27017"   # change if using Atlas or an external cluster
