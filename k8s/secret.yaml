# This file is intentionally kept in the repo as a reference only.
# The real Secret is created by CI (see .github/workflows/deploy.yml).
# Do NOT add real credentials here.
apiVersion: v1
kind: Secret
metadata:
  name: yeet-secret
type: Opaque
stringData:
  mongo-uri: "mongodb://mongo:27017"   # override in CI if using an external cluster
